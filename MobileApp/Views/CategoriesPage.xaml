<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MobileApp.Views.CategoriesPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewmodels="clr-namespace:MobileApp.ViewModels">

    <Grid RowDefinitions="Auto, *, Auto"
          Padding="20">

        <!-- Верхня частина (форма) -->
        <ScrollView Grid.Row="0">
            <VerticalStackLayout Spacing="10">

                <Label Text="Категорії"
                       FontSize="28"
                       HorizontalOptions="Center"/>

                <Frame Padding="15"
                       BackgroundColor="#f5f5f5"
                       CornerRadius="12">

                    <VerticalStackLayout Spacing="8">

                        <Entry Placeholder="Назва категорії"
                               Text="{Binding NewCategoryName}"/>

                        <Entry Placeholder="Податок (%)"
                               Keyboard="Numeric"
                               Text="{Binding NewCategoryTaxAmount}"/>

                        <Picker Title="Тип податку"
                                ItemsSource="{Binding TaxTypes}"
                                SelectedItem="{Binding NewCategoryTaxType}"/>

                        <Button Text="Додати категорію"
                                Command="{Binding AddCategoryCommand}"/>
                    </VerticalStackLayout>
                </Frame>
            </VerticalStackLayout>
        </ScrollView>

        <!-- ПРОКРУЧУВАНИЙ СПИСОК -->
        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Categories}">
            <CollectionView.ItemTemplate>
                <DataTemplate>

                    <Grid Padding="10"
                          ColumnDefinitions="*, Auto, Auto"
                          ColumnSpacing="12">

                        <VerticalStackLayout Grid.Column="0"
                                             Spacing="3">
                            <Label Text="{Binding Name}"
                                   FontSize="18"
                                   FontAttributes="Bold"/>

                            <Label Text="{Binding TaxAmount, StringFormat='Податок: {0:F2}%'}"
                                   FontSize="14"
                                   TextColor="Gray"/>

                            <Label Text="{Binding TaxType}"
                                   FontSize="14"
                                   TextColor="Gray"/>
                        </VerticalStackLayout>

                        <Button Grid.Column="1"
                                Text="✎"
                                WidthRequest="40"
                                HeightRequest="40"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:CategoriesViewModel}}, Path=EditCategoryCommand}"
                                CommandParameter="{Binding .}"/>

                        <Button Grid.Column="2"
                                Text="🗑"
                                TextColor="Red"
                                WidthRequest="40"
                                HeightRequest="40"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:CategoriesViewModel}}, Path=DeleteCategoryCommand}"
                                CommandParameter="{Binding Id}"/>

                    </Grid>

                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>
</ContentPage>
