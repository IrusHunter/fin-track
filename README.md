# FinTrack

## 📄 Опис  
FinTrack — це універсальний застосунок для керування особистими / сімейними фінансами. Він об’єднує веб-інтерфейс, мобільний застосунок та бекенд, тож дає змогу:  
- відслідковувати доходи та витрати,  
- планувати бюджети,  
- переглядати фінансову статистику,  
- зберігати/синхронізувати дані між пристроями,  
- (за бажання) розширювати функціонал під свої потреби.

Проєкт складається з кількох модулів: WebApp, MobileApp, API / бекенд, а також містить можливості для автоматизованих тестів.

## 🚀 Структура проєкту  
```

/
├── WebApp/         # фронтенд для браузера
├── MobileApp/      # фронтенд для мобільних пристроїв
├── ConsoleApp/     # консольний інтерфейс (якщо потрібен)\
├── Tests/          # тести
├── docker/         # Docker / docker-compose конфігурації
├── .env.example    # приклад конфігурації середовища
├── Dockerfile      # для контейнеризації
└── …               # інші конфігураційні файли, налаштування, скрипти

````

## 🧰 Технічний стек  
- Основна мова: **C#** для бекенду / API, **HTML / CSS / JS (якщо є веб-інтерфейс)** для фронтенду.  
- Можливе використання Docker + docker-compose для швидкого підняття проєкту.  
- Конфігурація через `.env` для зручної зміни налаштувань (база даних, секрети тощо).  

## 📥 Початок роботи (для локального запуску)  
> Передбачається, що у вас встановлені Docker та Docker-Compose.  

1. Клонувати репозиторій  
   ```bash
   git clone https://github.com/IrusHunter/fin-track.git
   cd fin-track


2. Скопіювати приклад конфігурації

   ```bash
   cp .env.example .env
   ```

   та заповнити необхідні значення (база даних, порти, секрети тощо).
3. Запустити контейнери

   ```bash
   docker-compose up --build
   ```
4. Відкрити застосунок (веб або мобайл, залежно від налаштування).

## 📄 Ліцензія

Проєкт поширюється під **MIT License** (див. файл `LICENSE`).

## 🤝 Внесок

Вітаються pull request, баг-репорти, пропозиції щодо нових фіч або покращення існуючого коду.
