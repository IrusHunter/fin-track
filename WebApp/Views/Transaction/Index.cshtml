@model FinTrack.Models.ViewModels.TransactionSearchViewModel
@{
    ViewData["Title"] = "Transactions";
}

<div class="item-container">
    <h2>💰 Транзакції</h2>
    <hr />

    <a asp-action="Create" class="btn btn-success mb-3">➕ Додати транзакцію</a>

    <partial name="_SearchPartial" model="Model" />

    @if (Model.Transactions == null || !Model.Transactions.Any())
    {
        <div class="alert alert-info">Не знайдено жодної транзакції.</div>
    }
    else
    {
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Назва</th>
                    <th>Сума</th>
                    <th>Сума після податку</th>
                    <th>Категорія</th>
                    <th>Користувач</th>
                    <th>Створено</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var t in Model.Transactions)
                {
                    <tr>
                        <td>@t.Id</td>
                        <td>@t.Name</td>
                        <td>@t.Sum</td>
                        <td>@t.SumAfterTax</td>
                        <td>@t.Category?.Name</td>
                        <td>@t.User?.UserName</td>
                        <td>@t.CreatedAt.ToLocalTime()</td>
                        <td>
                            <a asp-action="Update" asp-route-id="@t.Id" class="btn btn-warning btn-sm">✏️ Редагувати</a>
                            <form asp-action="Delete" asp-route-id="@t.Id" method="post" style="display:inline;">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-danger btn-sm"
                                    onclick="return confirm('Видалити цю транзакцію?')">🗑️ Видалити</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>